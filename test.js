const identity = require('clear-me-identity').identity;
// const stringified = JSON.stringify(JSON.parse(identity));
console.log(`DEBUG: identity: ${identity.abi[0]}`);
// console.log(`DEBUG: stringified: ${stringified}`);
